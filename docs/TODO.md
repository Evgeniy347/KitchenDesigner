# Что нужно сделать — хвосты сессии 2026-09-11

Документ для следующей сессии: только незакрытое. Дорожная карта части 3 живёт отдельно,
в `docs/todo_evolution.md`. Формат: **что → зачем → на что ложится → цена → ловушка**.

---

## P1 — от пользователя

### 1. ЛАГИ: что закрыто и что осталось померить

**Закрыто по дампу F9 с живого проекта пользователя** (`perf_20260911_163010.csv`, 839
кадров, 401 деталь). Дамп назвал виновников за один заход, а чтение кода до него называло
неверных:

- `SceneAnalyzer.Analyze` — **39 вызовов, каждый 676,6 мс**. Заслонка по времени работала
  честно, но прятала цену: в среднем по кадрам это выглядело как 31 мс и не выглядело как
  заморозка на две трети секунды. Внутри `ConstraintValidator.FindNearContacts` каждая пара
  деталей шла в полный перебор граней 6×6 без широкой фазы — 80 200 пар. Стало
  **243,2 мс → 4,5 мс**, глубоких пар 80 200 → 380;
- `OpeningCollision.FindMaxProgress` — 260,3 мс в среднем на вызов. Дорог был СКАН, а не
  список препятствий (`BuildObstacles` — 1,63 мс). В `BoxOverlap.PenetrationUnits` было
  девяносто поворотов кватернионом на одну пару коробок. Стало **76,2 мс → 4,2 мс**;
- кадр перетаскивания с неподвижной мышью больше не считает ничего: валидаций 1 → 0,
  геометрий 401 → 0. Корень — запись в `transform` тем же значением поднимает `hasChanged`
  и обесценивает кэш по ревизии;
- мусор: 3,6 МБ на кадр в среднем оказались 39 всплесками собственного мусора `Analyze`
  (обычный кадр — 263 КБ). Отдельной охоты не требуется, но проверить следующим дампом.

**Осталось — по ВТОРОМУ дампу** (`perf_20260911_183920.csv`, 404 кадра, жалоба «fps 2–3, если
оставить открытым фасад»). Кадр подешевел вдвое (`dt_ms` 57,4 → 27,4), но:

1. **`SceneAnalyzer.Analyze` на реальной сцене по-прежнему ~470 мс за вызов** (470,3 / 469,2 /
   458,9 / 480,6 / 484,5 / 513,5 / 460,3 / 451,1). Замер на ядре давал 243,2 → 4,5 мс — значит
   починка не доехала до пути, которым ходит приложение, либо дорогая часть не в
   `FindNearContacts`. Мерить то, что зовёт приложение, а не то, что удобно померить.
2. **Открытый фасад заставляет сцену устаиваться раз в 17 кадров, непрерывно** — кадры 3000,
   3017, 3033, 3050, 3067, 3084, 3101. При этом `FacadeElement.StepDoor` за весь дамп не
   сработал НИ РАЗУ: анимация кончилась, фасад просто стоит открытым. `SceneChangeTracker.Poll`
   в эти кадры 3,0–4,1 мс против сотых долей обычно. Полсекунды заморозки на каждую треть
   секунды — это и есть 2–3 fps. Ищи писателя, который помечает неподвижную сцену
   изменённой; кандидат — запись в `transform` тем же значением (`hasChanged` поднимается).

И третье, прежнее: пока мышь ЕДЕТ, кадр платит ~4,1 мс снэпа и ~3,9 мс валидации ЯДРА на 401
детали. Убрать вторую — валидация по окрестности перетаскиваемого набора; **ловушка:**
`ValidationCore` считает и связность, и на подмножестве сцены она даёт ЛОЖНЫЕ нарушения
«нечем подпереть».

### 2. Связность без якоря выбирает корень по порядку списка

`ValidationCore.CheckConnectivity`, ветка `if (!hasAnchor)`: корень обхода — ПЕРВАЯ по списку
деталь с контактом. Значит в сцене без якоря деталь, попавшая в список раньше, забирает роль
«земли» себе, а прежняя земля целиком краснеет — статус меняется у деталей, которых никто не
трогал. Для кухни безвредно (пол — якорь), поэтому и дожило; но это единственное найденное
НЕЧИСТОЕ правило валидации, и от него недетерминирована полная валидация, а не только
инкрементальная. Закреплено тестом `SceneWithoutAnchors_ChangesStatusOfPartsTheMoverNeverTouches`
в `ValidationLocalityTests`.

### 3. Вызов MCP стоит 300–500 мс

Диагноз неполный: кэш валидации по ревизии не помог. Разбивка по этапам вызова пишется в
лог плеера — **следующий заход начинает с неё, а не с догадки**. Числа последней дымовой
проверки: `create_elements` 235 мс, `get_elements` 337 мс, `delete_elements` 93 мс,
`save_load_roundtrip` 880 мс. Собрать плеер, снять разбивку, чинить по ней.

### 4. `MmGrid.Snap` на загрузке пишет в данные пользователя

Единственное оставшееся место, где ОТКРЫТИЕ файла его меняет (в пределах 0,5 мм). Принцип
«загрузка чинит стык, а не размер» записан в `conventions/SERIALIZATION.md` и доведён для
опоры и трубы. Довести: несовпадение с сеткой становится находкой валидации либо
одноразовой версионной миграцией с явным согласием пользователя.

### 5. Таблица «регион → глубина промерзания»

Поле во вкладке «Строительство» показывает прочерк: список регионов есть, чисел нет.
Источник решён пользователем — **СП 131.13330**. Пока таблицы нет, правило `FND-01` написать
нельзя. Сейчас восемь макрорегионов (`ConstructionRegionTitles`); при появлении таблицы
список, скорее всего, придётся привести к её строкам — это шов, о котором помнить.

---

## Что НЕ считать долгом

- **Диаметры фитингов не отменяются** — они выводятся с трассы, единственный писатель
  `PipeFittingSizeLink`. Это замысел. Уже дважды порывались «починить».
- **`docs/example.save.json` грязный в рабочем дереве** — живой файл пользователя. Не
  трогать, не откатывать, не предлагать починку и не спрашивать про него.
- **Полоса вкладок на восьми подписях жмётся**, кегль уменьшен до 11 px. Лечение симптома;
  по-хорошему нужна вторая строка или иконки, но пользователь не жаловался.
