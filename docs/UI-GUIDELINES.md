# Свод правил UI (KitchenDesigner)

Обязателен для любого нового окна, панели и контрола. Если элемент нарушает
правило — он дефектный и переделывается, а не документируется.

## 1. Единицы и числа

- Линейные размеры и позиции — **всегда миллиметры**, целые. Никаких метров в UI.
- Углы — градусы, один знак после запятой.
- Время — секунды.
- Единица измерения — серым суффиксом **внутри поля** (`800 мм`), а не в подписи.
- Один формат на весь продукт: мм — целые, углы — `0,0`.

## 2. Применение изменений

- Изменения применяются **сразу**: Enter или потеря фокуса. Кнопок «Применить»
  не существует.
- Каждая мутация проходит через `CommandStack` (undo обязателен). Новое
  свойство без Command — дефект.
- Невалидный ввод: красная рамка поля, значение не откатывается молча.
  Esc — откат, Enter — применение.

## 3. Деструктивные действия

- Красная кнопка (`UIStyle.Danger`), отделена отступом ≥16 px, никогда не на
  всю ширину, не соседствует с частыми контролами.
- Либо подтверждение, либо undo-тост («Удалено: X [Отменить]»).
- Удаление группы/составного элемента — всегда через явный выбор
  («Расформировать» / «Удалить с содержимым»).

## 4. Глифы

Один глиф — одно значение:
- `✕` (кнопка в углу) — только «закрыть окно/панель»;
- удаление — красная кнопка со словом «Удалить» или красный `✕` в строке списка;
- `✓` — «включено» (чекбокс);
- `▸ / ▾` — свернуть/развернуть.
- Буквы `v`, `X`, `<` как псевдоиконки запрещены.

## 5. Подписи и формулировки

- Каждый контрол имеет подпись. Дропдаун без label запрещён.
- Формулировки позитивные: «Закрепить», а не «Запретить перемещение».
- Заголовки с прописной буквы. Аббревиатуры запрещены («Выравнивание»,
  а не «Выравн.»).
- Иконная кнопка обязана иметь tooltip.
- Язык данных = язык UI: автоимена русские и человекочитаемые, без размеров
  в имени (имя с протухшими размерами хуже отсутствия имени).

## 6. Структура формы (инспектор)

- Подписи слева в колонку фиксированной ширины, поля — во вторую колонку.
- Секции с заголовками-разделителями, единый порядок во всех инспекторах:
  **Имя → Размеры → Параметры типа → Положение → Материал → Свойства → Действия**.
- Read-only поле — иконка замка + tooltip «почему нельзя менять».
- Действия: «Дублировать» слева, «Удалить» (danger) справа, отделены отступом.

## 7. Окна

- Заголовок = тип + имя сущности («Фасад — Фасад 3»).
- Крестик закрытия у всех окон: 32×32, отступ 8 px от краёв.
- Паддинг контента 12 px со всех сторон, включая низ (ничего не «прилипает»).
- Сборка только из `WindowChrome` + библиотечных компонентов, не из сырого
  `UIFactory` по месту.

## 8. Размеры и ритм

- Минимальный хит-таргет 32 px.
- Вертикальный ритм кратен 8 px; между группами ≥16 px, внутри группы 8 px.

## 9. Состояния

- У каждой кнопки/поля: normal / hover / pressed / disabled / focused.
- Тогглы (тулбар, чекбоксы) показывают состояние визуально (нажатый фон,
  галочка), а не словом «вкл/выкл» в подписи.

## 10. Цвет и типографика

- Палитра только из `UIStyle`: фон, поверхность, акцент, danger, текст
  первичный/вторичный. Локальные цветовые константы запрещены.
- Размеры шрифта — из `UIStyle` (заголовок окна, заголовок секции, текст,
  вторичный текст).

## 11. Чек-лист ревью нового окна

- [ ] единицы — мм, формат чисел единый;
- [ ] у всех контролов подписи;
- [ ] все мутации через `CommandStack` (undo работает);
- [ ] деструктивные действия отделены и красные;
- [ ] крестик закрытия, паддинг низа;
- [ ] hover/disabled состояния;
- [ ] tooltip у иконных кнопок;
- [ ] снапшот добавлен в UI-тесты (`test-results/*.png`).

---

# План работ (чек-лист)

## Этап 0 — фундамент
- [ ] `UIStyle`: токены отступов, размеров, цветов, шрифтов; danger-цвет.
- [ ] Глифы ▸ ▾ ✕ ✓ в TMP-атласе; убрать буквы «v»/«X»-псевдоиконки.
- [ ] Компоненты: `Section`, `FormRow`, `NumberField(unit)`, `CheckRow`,
      `DangerButton`, `WindowChrome`.

## Этап 1 — единицы
- [ ] Позиция в мм (убрать метры), угол с «°».
- [ ] Красная рамка при невалидном вводе, Esc/Enter-семантика.

## Этап 2 — инспектор элемента
- [ ] Заголовок «Тип — Имя», крестик по правилу 7.
- [ ] Убрать безымянный дропдаун типа (тип — read-only текст).
- [ ] Секции по правилу 6.
- [ ] Зазоры: поля подписаны (слева/справа/сверху/снизу).
- [ ] Ящик: расшифровать «A (86 мм)»/«L=350», убрать дублирование ширины.
- [ ] Пазы: строки с параметрами и единицами, кнопка редактирования у каждой,
      операции через CommandStack.
- [ ] Убрать «Применить»; live-apply единственная модель.
- [ ] «Удалить» — danger, не на всю ширину, undo-тост.
- [ ] «Закрепить» вместо «Запретить перемещение», настоящие чекбоксы с ✓.
- [ ] Повороты на 90° — компактно рядом с полями углов.

## Этап 3 — панель «Сцена»
- [ ] Дерево: отступ 16 px/уровень, ▸/▾, подсветка выбранного.
- [ ] «+ Группа» отодвинута от крестика закрытия.
- [ ] «X» группы → меню «Расформировать» / «Удалить с содержимым».
- [ ] Нижний паддинг списка, заметный ресайз-грип.

## Этап 4 — тулбар и статус
- [ ] Группы с сепараторами: файл | undo | выравнивание | вид | панели.
- [ ] Тогглы с нажатым состоянием, без «вкл/выкл» в тексте.
- [ ] Убрать/заполнить «Режим: [ ]»; полные слова; тултипы.
- [ ] Статус-бар вместо висящего текста «Автосохранение вкл».

## Этап 5 — вспомогательные окна
- [ ] Настройки: зависимые поля disabled+отступ, «автосейв»→«автосохранение»,
      «Блокировать ошибки» переименовать, скрыть пустую «Графику», крестик.
- [ ] День/Ночь: паддинг низа, подпись «Азимут солнца», точный ввод времени.
- [ ] Спецификация: итоговая строка, колонки Ш|В|Г раздельно, сортировка,
      акцентный «Экспорт CSV», крестик.

## Этап 6 — регрессия
- [ ] `build.cmd -RunTests`, обновить снапшоты, коммит на каждый этап.
